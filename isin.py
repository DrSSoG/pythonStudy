
def sortString(x):
    tab = []
    for char in x:
        tab.append(char)
        print(char)

    tab.sort()
    x = tab
    ans = ''
    for el in x:
        ans += el
    return ans

def bisectionRecur():
    if len(x) % 2 == 0:
        if x[len(x)/2] == base:
            return base
        elif x[len(x)/2] < base:
        
print(sortString('bdackjoe'))
